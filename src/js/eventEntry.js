function createEventEntry(event, date, location) {
    return `
                <p>${task}</p>
                <p>${date}</p>
                <p>${location}</p>`;
    }
    module.exports = createEventEntry